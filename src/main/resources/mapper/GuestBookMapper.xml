<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.study.myproject01.guestbook.mapper.GuestBookMapper">
      <select id="selectAll" resultType="GuestBookVO">
          select * from guestbook where g_active = 0
      </select>
      <insert id="insertGuestBook" parameterType="GuestBookVO">
          insert into guestbook(g_writer, g_subject, g_content, g_email, g_pwd, g_regdate
           <if test="f_name != null">, f_name</if>  )
          values(#{g_writer},#{g_subject},#{g_content},#{g_email},#{g_pwd},now()
          <if test="f_name != null">, #{f_name} </if> )
      </insert>
      <select id="selectDetail" parameterType="GuestBookVO" resultType="GuestBookVO">
          select * from guestbook where g_idx = #{g_idx}
      </select>
      <update id="deleteGuestBook" parameterType="GuestBookVO">
          update guestbook set g_active = 1 where g_idx=#{g_idx}
      </update>
      <!--   f_name 에 내용이 있을 때만 update 하자    -->
      <update id="updateGuestBook" parameterType="GuestBookVO">
          update guestbook set g_writer=#{g_writer}, g_subject=#{g_subject},
          g_email=#{g_email}, g_content=#{g_content}
          <if test="f_name != null and f_name != ''">
              , f_name=#{f_name}
          </if>
          where g_idx=#{g_idx}
      </update>
</mapper>